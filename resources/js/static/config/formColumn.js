export const products = {
    columns: [
        'Sl', 'name','months', 'active', 'action'
    ]
};
export const lenders = {
    columns: [
        'Sl', 'logo', 'name', 'email', 'web site', 'active', 'action'
    ]
};

export const customers = {
    columns: [
        'Sl', 'name', 'email', 'gender', 'dob', 'applications', 'status', 'action'
    ]
};

export const applications = {
    columns: [
        'Sl', 'Name', 'Email', 'Applied', 'Status', 'Phone', 'Amount', 'Action'
    ]
};

export const offers = {
    columns: [
        'Sl', 'Lender', 'Reference', 'APR', 'Flat Rate', 'Monthly Payment', 'Status', 'Note', 'Action'
    ]
};
